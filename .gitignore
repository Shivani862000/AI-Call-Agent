node_modules/
.env
.env.*
# ...but the templates carry no secrets and are part of the deploy docs.
!.env.*.example
*.db
/tmp/
*.pdf
.DS_Store
cookies.txt
cookiejar
server.log
logs/
recordings/
backups/
*.wav
*.log
cleanup-backup-*
# Google service-account keys. These live on the droplet, never in git.
gmail-key.json
*-service-account*.json
*.iam.gserviceaccount.com.json
client_secret_*.json
# Backfill backups. These hold production call transcripts and patient names,
# and this repo is public. scripts/reanalyse-calls.js writes them before it
# overwrites any analysis; keep them on the machine that ran it.
scratch/reanalyse-backup-*.json
