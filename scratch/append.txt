const {
  pendingCallDiagnostics,
  PUBLIC_BASE_URL
} = require('./config');
const { toWssUrl } = require('./helpers'); // Or if it's already in helpers... Wait, if I append it to helpers I don't need to require it.
