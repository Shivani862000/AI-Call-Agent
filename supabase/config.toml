# Hosted-only Supabase CLI metadata. This project does not run `supabase start`
# or any local Docker services. Secrets and hosted project references stay in
# ignored local environment files or the deployment secret store.
project_id = "ai-call-agent"

[db]
major_version = 17

[db.migrations]
enabled = true
schema_paths = []

[db.seed]
enabled = false
sql_paths = []
