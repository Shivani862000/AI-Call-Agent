# P05 Recording and Callback Boundaries

**Goal:** Authenticated callbacks cannot turn recording download or playback into arbitrary network access, and every retrieval has bounded resource use.
**Spec:** [P05 remediation contract](../2026-09-08-APPLICATION_GAP_REMEDIATION_PLAN.md#p05--authenticate-callbacks-and-constrain-every-recording-fetch).
**Dependencies:** P01 isolated harness; existing legacy containment `d0f3f61` and media authorization `9710dd2`; [provider capability limits](../ICALLMATE_CAPABILITY_MATRIX.md).
**Architecture:** One streaming retrieval service validates configured origins and every DNS answer, pins the TLS connection to a validated address, and owns its deadline/byte limits. Pipeline and authenticated playback consume that service. Trusted storage uses object keys and configured storage origin.

## Global Constraints

- Existing `codex/remediation-fixes` checkout; schema stays `0019`; preserve other remediation and unrelated review documents.
- No live provider/database/storage calls, notifications, environment-file loading, pushes, merges or deployment. Use the audited P01 test harness and synthetic adapters.
- Recording/transcript/analysis access remains ADMIN-only. Authorize before lookup, retrieval or signed-URL creation; responses remain `private, no-store` including redirects and error paths.
- Empty recording origin configuration denies provider retrieval. Unknown provider support must stay explicit; do not invent signature, redirect or URL-suffix guarantees.
- Existing legacy callbacks default disabled and require the configured scalar secret when enabled. Preserve supported canonical authentication. P10 owns attempt correlation and replay safety; a shared secret alone does not complete that work.

## Verified implementation references

Read on 9 September 2026: the [IANA IPv4](https://www.iana.org/assignments/iana-ipv4-special-registry/) and [IPv6 special-purpose registries](https://www.iana.org/assignments/iana-ipv6-special-registry/) distinguish globally reachable addresses from private, shared, documentation and protocol-specific space. Include easily missed cases such as `100.64.0.0/10`, IPv4-mapped/translation addresses, `2002::/16`, `2001:db8::/32` and the newer `3fff::/20` documentation block. Conservative rejection of special-purpose/translation ranges is acceptable for this exact-provider-origin client; do not mistake a library's broad “unicast” label for the complete policy.

The [Node 24 HTTP API](https://nodejs.org/docs/latest-v24.x/api/http.html#httprequesturl-options-callback) supports a custom `lookup` and per-request connection isolation. Its timeout option only emits an event; explicit abort/destruction is still required. The [HTTPS API](https://nodejs.org/docs/latest-v24.x/api/https.html#httpsrequesturl-options-callback) retains TLS options. Implement a total deadline starting before DNS; a late DNS result must not start a connection after cancellation. Avoid a global pooled/proxy connector that bypasses the pinned-address contract, and test this production connector boundary with inert adapters.

### Task 1: Bound callback recording metadata and all retrieval paths

**Create:** `services/recording-fetch.js`, `test/recording-fetch.test.js` and focused actual route/pipeline integration tests.
**Modify:** `src/api-routes.js`, `src/icallmate-webhook.js`, `services/post-call-pipeline.js`, `services/supabase-storage.js`, configuration examples, audited test manifest.

1. Establish failing behavioral cases for both current `fetch` paths: pipeline `downloadRecording` and GET `/api/calls/:callId/recording`. Capture calls through synthetic network/storage adapters so no request escapes. Existing code directly fetches arbitrary stored URLs and buffers complete responses; the regression must exercise those callers, not only a new helper.
2. Add strict callback shape/type/length validation before mutation. Accept the explicitly supported scalar provider fields, reject arrays/objects for identifiers/status/recording references and invalid nonempty timestamps, and bound accepted bodies. Authentication must precede sensitive processing. Constrain recording metadata independently of a valid secret. Do not append `.mp3` to a complete URL or its query string; the provider contract has no verified suffix-rewrite rule. Keep external compatibility unverified until representative provider fixtures are available.
3. Parse an explicit configured list of exact HTTPS origins (including exact ports). Reject credentials, IP literals, malformed URLs and origins outside that list. Validate all resolved IPv4/IPv6 addresses against global-unicast policy, including mapped IPv4, loopback, private, link-local, metadata, multicast, unspecified and mixed safe/unsafe answers. Pin each connection to a vetted address while preserving hostname verification and TLS server name. Do not separately check DNS and then call an unpinned `fetch`.
4. Default to rejecting redirects because provider behavior is unverified. If explicit redirect support is configured, independently repeat origin/DNS/TLS validation for every hop with a hard ceiling of three redirects. No credentials are forwarded. Bound the entire request/redirect/body lifecycle to 30 seconds and 25 MiB initially; validate configurable lower limits. Reject unsupported content types, unexpected content encoding and conflicting/oversized lengths; enforce the byte bound while streaming even without a length header. These are conservative local limits, not a claim that provider capacity has been measured.
5. Make pipeline download use a unique runner/process-owned temporary filename independent of provider input, stream with backpressure, and remove partial files on failure. Keep P12 storage-based recovery separate. Make playback stream with backpressure and cancel/destroy upstream on browser disconnect or HEAD handling. Return safe fixed error summaries without signed URLs, raw provider body text or arbitrary DNS/driver messages.
6. Validate storage object keys as keys, encode path segments, and build storage requests only from trusted configured base/bucket. Signed URL responses must resolve to that configured origin and expected storage path; reject arbitrary returned origins, query/path injection and traversal. Keep authorization before signing. Add deadlines/response bounds appropriate to small signing responses, no redirect credential forwarding, and no signed-query logging.
7. Test allowed streaming, forbidden origins/addresses, IPv6/mapped forms, mixed DNS, pinned-address rebinding protection, redirect rejection/loop/limit, oversized/slow streams and cancellation. Assert forbidden requests never reach a network connector. Exercise both pipeline and actual authenticated playback, including unauthorized no-effects and stored-object signing. Tests may inject DNS/TLS connectors at the service boundary; production defaults must use the real validator and pinned connector.
8. Run affected audited suites and existing authorization/privacy/callback regressions; record counts and limitations, run `git diff --check`, commit only this task's named files and Tier 2 evidence. Controller owns the main evidence register and independent review. Do not spawn subagents.

Rollback: revert code/config changes without altering stored call records. Recording retrieval remains gated until exact provider origins and representative fixtures are verified. Callback replay and transport/disposition races remain P10 work.

## Task 1 execution evidence — 9 September 2026

Implemented on `codex/remediation-fixes` from `0177465f825ad7d4ac0081f8d81baba1b1f9b72f`; schema remains `0019`.

- Both real consumers use `services/recording-fetch.js`. Production constructs isolated HTTPS requests with exact-origin validation, all-answer DNS checks, a pinned lookup, original hostname/SNI and TLS certificate verification. It rejects literal hosts, credentials, special IPv4/IPv6 ranges, malformed URLs and mixed DNS. There is no unpinned fetch fallback.
- Defaults are an empty origin list, no redirects, 30 seconds for DNS through body consumption, and 25 MiB. Numeric overrides only lower the limits. Explicit redirects repeat validation and stop after three hops. Audio MIME, encoding, duplicate/conflicting lengths and streamed byte counts are checked. Request/body/downstream cancellation and fixed error summaries bound failure paths.
- The pipeline writes with backpressure to a fresh process-prefixed `mkdtemp` directory and an exclusive mode-0600 file independent of provider identifiers; failed retrieval removes its partial directory. Playback streams with backpressure; HEAD does no retrieval/signing and disconnect aborts upstream. ADMIN guards still precede lookup/signing/network. A narrow pre-auth recording middleware ensures denied playback also has `private, no-store`.
- Canonical authentication now precedes callback validation/inbound inspection. Legacy callbacks remain disabled by default and scalar-secret authenticated when enabled. Validation rejects nested/array fields, bracket aliases, controls, oversized fields/bodies, invalid nonempty timestamps and disallowed recording references before mutation. Accepted scalar fields are bounded at 256 characters (recording references 4096), bodies/query objects at 16 KiB/64 fields. Unknown bounded scalar fields remain ignored. Complete recording URLs and query strings are preserved without suffix rewriting.
- Storage operations validate object keys, encode each path segment, use the trusted configured HTTPS storage base/bucket, and refuse redirects with service credentials. Signing responses are limited to 64 KiB and five seconds; upload responses use the same size bound and a 30-second deadline. Signed URLs must have the configured origin, exact expected object path and one nonempty token parameter; traversal, encoded traversal, fragments and extra query fields are refused. Provider/driver text and signed queries are not included in errors.

### Verification

The first audited consumer run failed all three behavioral cases as intended: pipeline and authenticated playback both reached an inert `fetch` for `https://evil.invalid/a`; structured canonical input reached processing instead of returning a pre-effect 400. Initial storage cases also failed for unencoded keys, accepted traversal and raw provider-error disclosure. No external request escaped; the P01 provider guard remained loaded.

Final `npm run test:unit` passed **336/336**, zero failures, cancellations or skips. This includes recording retrieval **10/10**, actual recording route/pipeline/callback integration **7/7**, storage **4/4**, and all existing authorization, session/privacy, media, callback and storage-config regressions. The isolated HTTP tests used approved owned loopback listeners. DNS/TLS and storage fixtures replace inert adapter boundaries while the production validation and request options execute. Signing deadline tests shorten only the injected clock delay and assert the production five-second deadline. No database or live provider/storage service was accessed; no DB/schema behavior changed, so no DB suite was required. `git diff --check` passed.

Self-review found and corrected raw upstream stream-error propagation, percent-encoded signing-path traversal, malformed HTTPS normalization, and the missing private cache directive on denied playback. Regressions cover these cases. The old authorization registration fixture now supplies synthetic provider credentials because canonical authentication moved into middleware; the actual HTTP fixture still exercises credential rejection. Current-account cache invalidation is explicit in the new role-change denial case.

### Activation gate and remaining ownership

Retrieval remains disabled until exact provider origins, representative callback/audio fixtures, MIME behavior and any redirect requirements are verified. The origins and capacity are not guessed; 30 seconds/25 MiB are conservative local limits. The callback scalar/time constraints and complete-URL behavior are locally verified, while external compatibility remains unverified. DNS cancellation prevents any late connection but cannot stop the operating system's already-running resolver work. Stream failures after response headers close the response rather than emitting a second JSON response.

P10 still owns callback replay and attempt correlation. P12 still owns stored-audio recovery and the successful-file lifetime (including the existing upload/unlink-before-transcription behavior); this task only guarantees failed-download partial-file cleanup. No migration, live credential read, call, notification, push, merge or deployment occurred. User-owned untracked review documents were preserved.

Supabase references checked: [changelog](https://supabase.com/changelog) (the `.md` endpoint was unsupported by the reader), [signed URL reference](https://supabase.com/docs/reference/javascript/file-buckets-createsignedurl), and [storage REST reference](https://supabase.com/docs/reference/self-hosting-storage). The visible recent breaking changes did not alter this existing Storage REST signing contract. Only local adapters were used; these references are not evidence of provider activation or a live storage test.
